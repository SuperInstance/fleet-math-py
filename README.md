# Fleet Math Py


Python bindings for fleet-math-c. Same API as C version. 12x slower (measured, intentional for prototyping). `pip install fleet-math`. Use `--benchmark` flag for C comparison.

## License

Apache 2.0 — Cocapn fleet infrastructure.
